# Darwin E2E Test Configuration
# Copy this file to .env and fill in your actual values

# Darwin API Configuration
E2E_ENVIRONMENT=
E2E_API_KEY=
E2E_TEAM=

# --- Readonly Video Tests ---
# These are required for running readonly video registration E2E tests.
# In CI, AWS credentials are configured via OIDC. For local development,
# set these environment variables.

# Name of the external storage configuration in Darwin
E2E_STORAGE_NAME=darwin-e2e-data

# AWS Credentials for S3 Access (Local Development)
# ===================================================
# OPTION A: Long-term IAM User Credentials
# Use if you have a dedicated IAM user for testing
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=eu-west-1

# OPTION B: Temporary Credentials (AWS SSO/Console)
# Use when copying from AWS Console "Command line or programmatic access"
# or AWS SSO. Session token is REQUIRED for temporary credentials.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_REGION=eu-west-1

# Note: Temporary credentials expire (typically 1-12 hours).
# If AWS_ACCESS_KEY_ID starts with "ASIA", you MUST provide AWS_SESSION_TOKEN.
